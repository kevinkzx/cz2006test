import React from "react";
import {SearchTitle} from "./ParlourElement";

const Title = ({title}) => {
	return (
		<SearchTitle>
			<h4>{title}</h4>
			<div/>
		</SearchTitle>
	);
};

export default Title;